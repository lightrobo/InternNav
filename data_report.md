# InternNav 数据集报告

## 数据结构概览

```
data/
├── Embodiments/                  # 机器人模型 (77 MB)
│   └── vln-pe/h1/
│       ├── h1_internvla.usd
│       └── policy/
├── scene_data/
│   └── mp3d_pe/                  # 90个 Matterport3D 3D场景 (47 GB)
│       ├── 17DRP5sb8fy/
│       ├── 1LXtFkjw3qL/
│       └── ...
└── vln_pe/
    ├── raw_data/                 # JSON 任务定义文件
    │   ├── r2r/
    │   │   ├── mini/
    │   │   ├── train/
    │   │   ├── val_seen/
    │   │   └── val_unseen/
    │   └── interiornav/
    │       ├── train/
    │       ├── val_seen/
    │       └── val_unseen/
    └── traj_data/                # 训练轨迹数据 (107 GB) ⚠️ 需要解压
        ├── r2r/                  # 61个 tar.gz (89 GB)
        │   ├── 7y3sRwLe3Va.tar.gz
        │   └── ...
        └── interiornav/          # 21个 tar.gz (18 GB)
            ├── kujiale_0003.tar.gz
            └── ...
```

---

## 📊 数据量统计（实测验证）

### 磁盘占用 ✅ 已验证

| 数据类型 | 场景/文件数 | 大小 |
|---------|------------|------|
| 3D 场景 (`scene_data/mp3d_pe/`) | 90 个场景 | **47 GB** |
| R2R 轨迹 (`traj_data/r2r/`) | 61 个 tar.gz | **89 GB** |
| InteriorNav 轨迹 (`traj_data/interiornav/`) | 21 个 tar.gz | **18 GB** |
| 机器人模型 (`Embodiments/`) | - | 77 MB |
| **总计** | - | **~154 GB** |

### 轨迹数量统计 ✅ 已验证

#### raw_data 中的任务数量（从 JSON 实测）

| Dataset | Train | Val Seen | Val Unseen |
|---------|-------|----------|------------|
| R2R | **10,819** | 778 | 1,839 |
| InteriorNav | 649 | 44 | 99 |
| **Total** | **11,468** | **822** | **1,938** |

#### traj_data 中的轨迹数量（从 tar.gz 实测）

| Dataset | 轨迹数 |
|---------|--------|
| R2R | **2,813** |
| InteriorNav | **646** |
| **Total** | **3,459** |

### ⚠️ 数据不匹配说明

| 对比项 | raw_data (任务) | traj_data (轨迹) | 差异 |
|--------|----------------|------------------|------|
| R2R Train | 10,819 | 2,813 | 只有 ~26% |
| InteriorNav Train | 649 | 646 | 基本一致 |

**原因分析**：
- `raw_data` 是任务定义（起点、终点、指令），用于评估
- `traj_data` 是实际采集的轨迹数据，用于训练
- R2R 的 traj_data 可能只包含部分轨迹（采样或子集）
- README 中"8,679 (stair-filtered)"指的是过滤楼梯后的数量

---

## 🗂️ traj_data 内部结构（LeRobot 格式）

每个 `.tar.gz` 解压后的结构：

```
场景ID/
├── 轨迹ID_1/
│   ├── videos/chunk-000/
│   │   ├── observation.images.rgb/rgb.npy      # RGB 图像序列
│   │   └── observation.images.depth/depth.npy  # 深度图序列
│   ├── meta/
│   │   ├── episodes.jsonl    # 轨迹元信息
│   │   ├── tasks.jsonl       # 导航指令
│   │   └── info.json
│   └── data/chunk-000/
│       └── episode_000000.parquet  # 动作、位姿等结构化数据
├── 轨迹ID_2/
│   └── ...
└── ...
```

**⚠️ 重要**：这些 `.tar.gz` 文件 **需要解压** 才能被训练代码使用！

---

## 💡 为什么"场景少但数据量大"？

**场景数量少 ≠ 数据量少**

| 概念 | 数量 | 说明 |
|-----|------|------|
| 3D 场景 | 90 个 | 90 栋不同的建筑 |
| 轨迹包 | 82 个 tar.gz | 在 82 个场景里录制了轨迹 |
| 实际轨迹 | 3,459 条 | 3,459 段导航录像 |
| 每条轨迹 | 几十~上百帧 | RGB-D 连续帧序列 |

**结论**: 154 GB 的数据量合理，每条轨迹都是完整的多模态时序数据。

---

## 数据说明

### 1. 3D 场景数据 (`scene_data/mp3d_pe/`)

- **来源**: Matterport3D 数据集
- **内容**: 每个目录是一个完整的室内扫描场景
  - mesh 网格文件
  - 纹理贴图 (jpg)
  - 场景元数据
- **用途**: 在 Habitat/Isaac Sim 中加载 3D 环境进行仿真评估

### 2. R2R 轨迹数据 (`vln_pe/traj_data/r2r/*.tar.gz`)

- **格式**: 每个 `.tar.gz` 文件对应一个场景的导航轨迹
- **命名**: 文件名即场景 ID（如 `7y3sRwLe3Va.tar.gz`）
- **内容**: 机器人按照语言指令导航时采集的数据
  - RGB 图像 (npy)
  - 深度图 (npy)
  - 动作序列 (parquet)
  - 位置/姿态信息 (parquet)
- **用途**: 用于训练 VLN 模型（模仿学习）

### 3. InteriorNav 轨迹数据 (`vln_pe/traj_data/interiornav/*.tar.gz`)

- **来源**: Kujiale 高质量建模环境
- **格式**: 与 R2R 相同
- **场景命名**: `kujiale_XXXX`

### 4. 任务定义 (`vln_pe/raw_data/`)

- **格式**: JSON.gz 文件
- **内容**: 定义导航任务的起点、终点、语言指令
- **数据集划分**:
  - `train/`: 训练集
  - `val_seen/`: 验证集（已见场景）
  - `val_unseen/`: 验证集（未见场景）
  - `mini/`: 快速验证用小数据集

### 5. 机器人模型 (`Embodiments/`)

- **内容**: Unitree H1 人形机器人的 USD 模型和运动策略
- **用途**: 在 Isaac Sim 中加载机器人进行物理仿真

---

## ⚠️ 使用前必做

### 1. 解压轨迹数据

```bash
# 解压所有 R2R 轨迹
cd /mnt/data/yuxuan/repos/InternNav/data/vln_pe/traj_data/r2r/
for f in *.tar.gz; do tar -xzf "$f" && rm "$f"; done

# 解压所有 InteriorNav 轨迹
cd /mnt/data/yuxuan/repos/InternNav/data/vln_pe/traj_data/interiornav/
for f in *.tar.gz; do tar -xzf "$f" && rm "$f"; done
```

### 2. 确认场景路径

代码期望路径是 `data/scene_data/mp3d_pe/`。

如果数据在 `data/scene_data/scans/`，需要调整：

```bash
# 方案1：重命名
mv data/scene_data/scans data/scene_data/mp3d_pe

# 方案2：创建软链接
ln -s scans data/scene_data/mp3d_pe
```

---

*报告生成时间: 2026-01-15*
*数据验证方式: du -sh, ls | wc -l, zcat + python, tar -tzf*
